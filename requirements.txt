# ── Core ─────────────────────────────────────────────────────────────────────
pyyaml>=6.0
pydantic>=2.7

# ── AI / Ollama ───────────────────────────────────────────────────────────────
ollama>=0.3          # official Python client for Ollama

# ── Web dashboard ─────────────────────────────────────────────────────────────
fastapi>=0.111
uvicorn[standard]>=0.29

# ── Storage ───────────────────────────────────────────────────────────────────
# SQLite ships with Python — no extra package needed.

# ── Hardware (Pi only — not needed on Mac) ───────────────────────────────────
# Most drivers install into the venv with pip:
#   pip install spidev gpiozero w1thermsensor pillow
# picamera2 CANNOT be pip-installed (libcamera has no wheel). It ships as the
# apt package python3-picamera2; expose it to the venv by setting
#   include-system-site-packages = true
# in .venv/pyvenv.cfg.
# spidev>=3.6          # SPI access for MCP3008
# RPi.GPIO>=0.7        # GPIO control (gpiozero wraps this)
# gpiozero>=2.0        # relay / actuator control
# picamera2            # camera capture — apt: python3-picamera2 (system site-packages)
# w1thermsensor>=2.3   # DS18B20 1-wire temperature probe
# Pillow>=10.0         # image processing for camera capture

# ── Dev / test ────────────────────────────────────────────────────────────────
pytest>=8.0
pytest-asyncio>=0.23
httpx>=0.27          # async test client for FastAPI
